const form=document.querySelector('.form-cadastro');const inputs=form.querySelectorAll('input, select');const mensagem=document.getElementById('form-info');const lista=document.getElementById('lista-cadastros');function renderizarCadastros(){const cadastros=JSON.parse(localStorage.getItem('cadastros'))||[];lista.innerHTML='';if(cadastros.length===0){lista.innerHTML='<p>Nenhum cadastro ainda.</p>';return}
const ul=document.createElement('ul');cadastros.forEach(c=>{const li=document.createElement('li');li.textContent=c.nome;ul.appendChild(li)});lista.appendChild(ul)}
form.addEventListener('submit',(e)=>{e.preventDefault();const cadastro={nome:form.nome.value,email:form.email.value,cpf:form.cpf.value,telefone:form.telefone.value,nascimento:form.nascimento.value,endereco:form.endereco.value,cep:form.cep.value,cidade:form.cidade.value,estado:form.estado.value,};const cadastros=JSON.parse(localStorage.getItem('cadastros'))||[];cadastros.push(cadastro);localStorage.setItem('cadastros',JSON.stringify(cadastros));form.reset();renderizarCadastros()});renderizarCadastros()